# An example table file

# Header, not used by EvilUPS
File=Table
Product=example # Product name

# The group definition below groups together a 
# series of common actions for a series of flavours
#*************************************************
# Starting Group definition
Group:
# The flavours...
Flavor=Linux
Qualifiers=""
Flavor=OSX
Qualifiers=""

# The common actions
Common:
   Action=setup # The action keyword not used by EvilUPS, but is supported by UPS
# Some commands
      setupRequired("-f ${UPS_PROD_FLAVOR} tclx v7_4adf")
      proddir()
      setupenv()
# You can add more commands here...
End:
# End Group definition
#************************************************

# You can have multiple group definitions....