envAppend(PATH, ${PRODUCT_DIR}/bin)
envAppend(PYTHONPATH, ${PRODUCT_DIR}/bin)
envAppend(EUPS_PATH, @EUPS_PATH@)

addAlias(setup,   eval `${PRODUCT_DIR}/bin/eups_setup $@`);
addAlias(unsetup, eval `${PRODUCT_DIR}/bin/eups_setup --unsetup $@`);
